node_modules
dist
dist-ssr
*.local
.DS_Store
.vite
coverage

# secrets / env (e.g. Supabase keys) — never commit
.env
.env.*
!.env.example
# Ships in the public bundle anyway; see the file's own comment.
!.env.production
.gstack/
